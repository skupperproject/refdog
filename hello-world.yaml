version: 1
site:
  name: east
  links:
    - token-file: west-token-1.yaml
  provided-services:
    - name: backend
      workload: deployment/backend
      ports:
        - port: 8080
---
version: 1
site:
  name: west
  tokens:
    - token-file: west-token-1.yaml
  required-services:
    - name: backend
      ports:
        - port: 8080
